name: time_picker_wheel
description: Time Picker inspired by Oppo Clock app set alram time screen with full customization
version: 0.1.1
homepage: https://github.com/burhankhanzada/time_picker_wheel

topics:
  - time
  - picker
  - wheek

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl:
  provider:
  spaces2:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  intl_utils:

flutter_intl:
  enabled: true
  class_name: TimeTranslations
  main_locale: es
